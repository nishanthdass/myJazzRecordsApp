{{! Below form event allows for users to input an insert into database. Form id is used to refer to event handler in
relvant helper file.}}
<div class="container-fluid bg-dark text-white sticky-top" id="insert-bar">
    <form id="add-performance" class="px-4 py-3">
        <div class="row">
            <div class="col-sm">
                <input type="text" class="form-control form-control-lg form-control-block" placeholder="first name"
                    name="input-perfFn" id="input-perfFn">
            </div>
            <div class="col-sm">
                <input type="text" class="form-control form-control-lg form-control-block" placeholder="last name"
                    name="input-perfLn" id="input-perfLn">
            </div>
            <div class="col-sm">
                <input type="text" class="form-control form-control-lg form-control-block" placeholder="album name"
                    name="input-albName" id="input-albName">
            </div>
            <div class="col-sm">
                <button type="submit" class="btn btn-primary btn-lg btn-block"><i class="bi bi-plus-lg">
                    </i>Insert Performance</button>
            </div>
        </div>
    </form>
</div>

{{! Table is receving data from the database to populate the rows. Table id is refered to in relevent helper file
to build new rows for inserts and to find the relevant row for update and delete }}
<table class="table table-dark table-striped table-bordered" id="performance-table">
    <thead>
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th>Action</th>
        </tr>
    </thead>
    {{! tbody id is referenced in main helper. It is used to implement the search present in the navbar}}
    {{! tr data-value is essential for indexing rows in helper functions}}
    <tbody id="myTable">
        {{#each data}}
        <tr data-value={{this.performance_id}}>
            <td>{{this.performance_id}}</td>
            <td>{{this.musicians_musician_id}}</td>
            <td>{{this.first_name}}</td>
            <td>{{this.last_name}}</td>
            <td>{{this.albums_album_id}}</td>
            <td>{{this.name}}</td>
            <td>
                {{! delete uses a onclick function present in helper file}}
                <button onclick="deletePerformance({{this.performance_id}})" class="btn btn-danger btn-small"><i
                        class="bi bi-trash3-fill"></i>Delete</button>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>